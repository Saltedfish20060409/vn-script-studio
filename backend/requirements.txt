# 运行时顶层依赖（精确锁定 → 可复现构建）。
# 传递依赖由 pip 解析；如需完全冻结（含传递依赖），用 pip freeze > requirements-lock.txt
# 并在 Dockerfile 里改 COPY requirements-lock.txt ./requirements.txt。
fastapi==0.141.1
uvicorn[standard]==0.52.3
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
alembic==1.19.1
pydantic==2.13.4
pydantic-settings==2.15.0
typing-extensions==4.16.0
# SECURITY: python-jose is unmaintained (CVE-2024-33663/33664 fixed but no
# upstream work); PyJWT is the maintained successor with the same API.
PyJWT==2.10.1
# SECURITY: passlib is unmaintained since 2020 and pins bcrypt to 4.0.1;
# use the bcrypt library directly (same $2b$ hash format — existing hashes
# keep verifying).
bcrypt==4.3.0
httpx==0.28.1
python-multipart==0.0.32
python-docx==1.2.0
typer==0.27.0
cryptography==49.0.0
email-validator==2.3.0
# Optional: cross-worker SSE bridge when REDIS_URL is set (multi-worker deploys)
redis==8.1.0
# scripts/eval_*.py load .env via python-dotenv (declared explicitly — it is
# currently only a transitive dep of pydantic-settings)
python-dotenv==1.2.2
