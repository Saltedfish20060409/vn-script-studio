# Legacy Next.js monorepo

This folder contains the previous TypeScript monorepo (`packages/core`, `packages/web`, `packages/cli`) kept for reference during the FastAPI + React migration.

**Do not use for day-to-day development.** Use `../backend` and `../frontend` instead.
