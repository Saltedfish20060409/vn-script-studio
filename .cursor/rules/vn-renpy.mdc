---
description: Ren'Py 视觉小说脚本约定（本仓库）
globs:
  - "**/*.rpy"
  - "**/project.json"
  - "packages/**/*.ts"
  - "packages/**/*.tsx"
alwaysApply: false
---

# VN Script Studio 规则

- 剧本默认面向 Ren'Py：优先合法 `.rpy` 语法，而不是好莱坞 screenplay 格式
- 角色 `define` 名用 `[a-z][a-z0-9_]*`；显示名可中文
- AI 续写/改写应尊重角色卡的 `voice` / `bio` 与项目 `lore`
- 共享逻辑在 `packages/core`；Web / CLI / Cursor Skill 不要分叉第二套导出格式
- 新功能优先进 `core`，再接到 web API 与 CLI
